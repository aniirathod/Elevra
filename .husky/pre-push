#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🧪 Running TypeScript type checks before PUSH..."

# 1. Run turborepo type-check across all workspaces
npm run type-check || { 
    echo "❌ Type-check failed. Push aborted."; 
    exit 1; 
}

echo "✅ Type-check passed. Safe to push!"
